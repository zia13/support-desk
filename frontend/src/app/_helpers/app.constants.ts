import { environment } from './environment';

export const AppConstants = {
  API_BASE_URL: environment.apiBaseUrl,
  SUPPORT_EMAIL: environment.supportEmail,
  FEATURE_TOGGLE: environment.featureToggle
};
